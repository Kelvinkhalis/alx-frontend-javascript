export function taskFirst() {
	const task = 'I prefer when i can.'; 
	return task;
}

export function getLast() {
	return is 'is okay';
}

export function taskNext() {
	let combination = 'but sometimes let';
	combination += getLast();

	return combination;
}
